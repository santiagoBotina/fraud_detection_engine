name: Decision Service CI

on:
  push:
    paths:
      - "ms-decision-service/**"
      - ".github/workflows/decision-service.yml"
  pull_request:
    paths:
      - "ms-decision-service/**"
      - ".github/workflows/decision-service.yml"

defaults:
  run:
    working-directory: ms-decision-service

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: ms-decision-service/go.sum

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest

      - name: Run golangci-lint
        run: golangci-lint run ./...

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: ms-decision-service/go.sum

      - name: Run tests
        run: go test -race -count=1 ./...
